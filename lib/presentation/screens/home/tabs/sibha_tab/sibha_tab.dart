import 'package:flutter/material.dart';

class SibhaTab extends StatelessWidget {
  const SibhaTab({super.key});

  @override
  Widget build(BuildContext context) {
        return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}